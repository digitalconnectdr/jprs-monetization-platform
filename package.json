{
  "name": "jprs-monetization-platform",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo de Decidero (nombre comercial provisional, ver docs/DECISIONS.md ADR-009) — Monetization Intelligence Platform.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
